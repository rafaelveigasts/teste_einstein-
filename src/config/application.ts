export const applicationConfig = Object.freeze({
  port: process.env.PORT as string,
  env: process.env.ENVIRONMENT as string,
  appPrefix: 'teste_einstein',
  name: 'Teste desenvolvedor backend pleno',
  version: '1.0.0'
})
